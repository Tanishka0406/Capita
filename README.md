# Capita

## Convertly

A dependency-free currency converter built with HTML, CSS, and JavaScript.

Open `index.html` directly in a browser to use it. The included rates are sample mid-market rates; connect the `currencies` object in `script.js` to a live rates API for production use.
